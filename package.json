{
  "name": "ponylab",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "validate:content": "tsx scripts/validate-content-paths.ts",
    "prebuild": "npm run validate:content",
    "check": "astro check",
    "test:unit": "vitest run",
    "test:e2e": "tsx scripts/run-e2e.ts",
    "build": "astro build && tsx scripts/prune-image-masters.ts",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.1.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@playwright/test": "^1.61.1",
    "@types/node": "^22.20.1",
    "tsx": "^4.23.1",
    "vitest": "^4.1.10",
    "yaml": "^2.9.0"
  }
}
