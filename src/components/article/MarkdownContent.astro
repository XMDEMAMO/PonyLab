---
import '../../styles/prose.css';

export interface Props {
  postId: string;
}

const { postId } = Astro.props;
---

<article
  class="article-prose"
  data-article-content
  data-pagefind-body
  data-pagefind-meta={`postId:${postId}`}
>
  <slot />
</article>
